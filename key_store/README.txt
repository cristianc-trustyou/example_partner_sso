Place in this directory SSH key pairs of the form:

priv_consumer_id.pem : private key used to generate the signed url
pub_consumer_id.pem: public key used to verify the signed url, not used in this example

Replace the "consumer_id" part of the file name with the actual consumer id.
